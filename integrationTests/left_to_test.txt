@DeleteMapping("/mascots/{mascot_id}")
@DeleteMapping("/posts/{post_id}")
@DeleteMapping("/threads/{thread_id}")
@DeleteMapping("/{user_id}")
@GetMapping("/api/test")
@GetMapping("/users/me/posts")
@GetMapping("/{user_id}")
@PatchMapping("/comments/{comment_id}")
@PatchMapping("/mascots/{mascot_id}")
@PatchMapping("/posts/{post_id}")
@PatchMapping("/threads/{thread_id}")
@PatchMapping("/users/me")
@PatchMapping("/users/me/password")
@PostMapping("")
@PostMapping("/admins")
@PostMapping("/login")
@PostMapping("/mascots")
@PostMapping("/posts")
@PostMapping("/posts/{post_id}/comments")
@PostMapping("/threads")

@GetMapping("/users/me") // 500 error that needs to be 401
@DeleteMapping("/users/me") // 500 error that needs to be 401
@DeleteMapping("/refresh") // bad token doesn't give right error

Hibernate error, can't delete users because of the refresh token foreign key
